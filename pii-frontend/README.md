# Gradio frontend for the pii-server 

To run this do the followings
* Install poetry (if you havne't on your system.)
* `cd pii-frontend`
* `poetry install`
* Run the server `poetry run python app.py`